# Normalize all text files to LF, regardless of the contributor's OS
# or editor settings. Several files had drifted to CRLF over time
# (mixed with the LF used everywhere else in the repo); this prevents
# that from recurring.
* text=auto eol=lf